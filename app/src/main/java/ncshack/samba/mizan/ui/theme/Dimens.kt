package ncshack.samba.mizan.ui.theme

import androidx.compose.ui.unit.dp

// Extracted from Figma design
object Dimens {
    // Button radius (from Figma: R5 radius)
    val ButtonRadius = 5.dp

    // Input field radius (from Figma: 10dp radius stroke)
    val InputRadius = 10.dp

    // Card radius (M3 medium shape token)
    val CardRadius = 12.dp

    // Spacing values
    val SmallSpacing = 8.dp
    val MediumSpacing = 16.dp
    val LargeSpacing = 24.dp
    val ExtraLargeSpacing = 32.dp

    // Padding values
    val SmallPadding = 8.dp
    val MediumPadding = 16.dp
    val LargePadding = 24.dp
    val ExtraLargePadding = 32.dp

    // Minimum touch target (M3 default)
    val MinTouchTarget = 48.dp
}